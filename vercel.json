{
  "ignoreCommand": "echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -q '\\[deploy\\]' && exit 1 || exit 0",
  "crons": [
    {
      "path": "/api/cron/auto-confirm-orders",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/finalize-group-buys",
      "schedule": "0 4 * * *"
    }
  ]
}
